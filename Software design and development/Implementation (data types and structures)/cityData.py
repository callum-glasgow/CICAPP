# Define the record structure using a class
class CityData:
    def __init__(self, city_name, population, area_km, capital):
        self.city_name = city_name
        self.population = population
        self.area_km = area_km
        self.capital = capital